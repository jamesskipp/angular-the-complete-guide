import { MouseoverLoggerDirective } from './mouseover-logger.directive';

describe('MouseoverLoggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseoverLoggerDirective();
    expect(directive).toBeTruthy();
  });
});
